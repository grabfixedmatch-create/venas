name: Daily WordPress Post Footy

on:
  schedule:
    - cron: '0 8 * * *'  # daily at 08:00 AM UTC
  workflow_dispatch:      # optional manual trigger

jobs:
  post:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 cloudscraper

      - name: Run scraper
        run: python footy_tips.py
        env:
          WP_USERNAME: "pettarr97@gmail.com"
          WP_APP_PASSWORD: "Drzj RwZt kqgj pjgg Q6WT omhE"
